#!/usr/bin/env python3
"""
Alpha Network SDK — setup.py (setuptools build).
For modern builds, use pyproject.toml directly.
This file exists for compatibility with legacy tooling.
"""
from setuptools import setup

setup()
